<mj-button
    background-color="{{ $color ?? '#5cb85c' }}"
    color="{{ $textColor ?? '#ffffff' }}"
    href="{{ $href }}"
    font-size="{{ $fontSize ?? '14px' }}"
    padding="{{ $padding ?? '10px 25px' }}"
>
    {{ $text }}
</mj-button>
